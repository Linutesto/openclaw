---
summary: "CLI reference for `openpaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `openpaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
openpaw uninstall
openpaw uninstall --all --yes
openpaw uninstall --dry-run
```
